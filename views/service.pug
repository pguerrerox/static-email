html
  head
    script(src="/socket.io/socket.io.js")
    script.
      var socket = io.connect('http://localhost:8000')
      socket.on('news', function (data) {
        console.log(data);
        document.getElementById('data').innerHTML = data.activityNumber;
        console.log('updated');
      })
  body
    h1  hola
    h3#data  #{activityNumber}

    if param === 'admin'
      h1 #{param}